```
{{ site.remote.bash_shebang }}
{{ site.sched.comment }} {{ site.sched.flag.name }} solo-job
{{ site.sched.comment }} -q serial

# Load the computing environment we need
module load python/3.10.10-gcc-9.4.0

# Activate virutal environment
source hpc-carpentry-amdahl-710cf57/.venv/bin/activate

# Execute the task
amdahl
```
{: .language-bash}
